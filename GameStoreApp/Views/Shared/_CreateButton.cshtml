@model string;

@if (User.Identity.IsAuthenticated && User.IsInRole("admin"))
{
    <div class="text-white CreateAddButton">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle"> Add New</i>
        </a>
    </div>
}

